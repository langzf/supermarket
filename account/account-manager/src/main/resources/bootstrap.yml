eureka:
  client:
    service-url:
      defaultZone: http://39.97.101.222:8761/eureka
server:
  port: 8083
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: SUPERMARKET-CONFIG-SERVER-LOC
  application:
    name: account-${ENV}
  profiles:
    active: ${ENV}